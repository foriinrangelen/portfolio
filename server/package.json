{
  "name": "portfolio2-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute portfolio-db --local --file=./src/db/schema.sql",
    "db:init:remote": "wrangler d1 execute portfolio-db --remote --file=./src/db/schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "reflect-metadata": "^0.2.2",
    "tsyringe": "^4.10.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250224.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.114.0"
  }
}
